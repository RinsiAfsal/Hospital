<h1 class="pt-8 text-3xl font-bold text-center">Edit Patient Details</h1>

<%= form_with(model: @patient, url: receptionist_patient_path, local: true) do |form| %>
  <%= render 'form', patient: @patient %>
<% end %>

<%= link_to 'Back to Patients List', receptionist_patients_path, class: 'hover:text-black text-gray-600 flex justify-center' %>

