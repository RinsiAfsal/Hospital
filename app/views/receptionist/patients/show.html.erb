<div class="max-w-4xl mx-auto bg-gray-100 p-8 mt-6 rounded-lg shadow-lg">
    <h1 class="text-3xl font-semibold text-gray-800 mb-6">Patient Details</h1>
    <div class="space-y-4">
        <p><strong class="font-semibold text-blue-700">First Name:</strong> <%= @patient.first_name %></p>
        <p><strong class="font-semibold text-blue-700">Last Name:</strong> <%= @patient.last_name %></p>
        <p><strong class="font-semibold text-blue-700">Date of Birth:</strong> <%= @patient.dob.strftime("%B %d, %Y") %></p>
        <p><strong class="font-semibold text-blue-700">Email:</strong> <%= @patient.email %></p>
        <p><strong class="font-semibold text-blue-700">Phone Number:</strong> <%= @patient.phone %></p>
    </div>

    <div class="mt-6 space-x-4">
       <%= link_to "Edit Patient", edit_receptionist_patient_path(@patient), class: "bg-blue-600 text-white py-2 px-6 rounded-lg hover:bg-blue-500 focus:outline-none" %>
       <%= link_to "Back to Patients List", receptionist_patients_path, class: "bg-gray-600 text-white py-2 px-6 rounded-lg hover:bg-gray-500 focus:outline-none" %>
    </div>
    <div class="mt-4">
       <%= button_to "Delete Patient", receptionist_patient_path(@patient), method: :delete, data: { turbo_confirm: "Are you sure you want to delete this patient?" }, class: "bg-red-600 text-white py-2 px-6 rounded-lg hover:bg-red-500 focus:outline-none" %>
    </div>
</div>